# Streamlit Cloud: paste this into App → Settings → Secrets (do NOT commit real secrets).
# Local dev: copy to .streamlit/secrets.toml and fill in, or use a .env file instead.

MOTHERDUCK_TOKEN = "your-motherduck-token"
MD_DATABASE = "refund_tracker"
